---
name: debug
description: Debug failures, investigate errors, perform root cause analysis. Use when something is broken or failing.
---

# Debug Issue

Investigate failures and perform comprehensive root cause analysis.

Invoke qa-agent in root-cause analysis mode to debug the issue. The agent will trace execution paths using Serena, identify the failure point, and suggest specific fixes with evidence.
